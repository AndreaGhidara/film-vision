import LoadNotFound from "@/components/custom/LoadNotFound";

export default function PageNotFound() {
    return (
        <div className="w-full h-screen flex items-center justify-center">
            <LoadNotFound />
        </div>
    )
}
